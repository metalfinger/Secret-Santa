# Enable shared leaderboard (optional)
VITE_REMOTE_LEADERBOARD=1

# Optional override (frontend uses its own constant; function can use EVENT_ID too)
# EVENT_ID=vmt-secret-santa-2025

# Netlify Function env vars (set these in Netlify UI, not in the frontend)
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
